# public

Static assets served as-is.

- `favicon.svg` — placeholder mark (a stylized "P"). Replace with the
  final brand icon and generate `favicon.ico` / `apple-touch-icon.png`
  before launch, then wire them in `src/lib/metadata.ts`.
- `og-image.png` — NOT YET ADDED. Referenced by `siteConfig.ogImage` in
  `src/constants/site.ts`; add a real 1200x630 OpenGraph image here
  before the SEO metadata is fully functional.
