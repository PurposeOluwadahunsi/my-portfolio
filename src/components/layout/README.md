# components/layout

Structural, page-independent chrome: navigation, footer, page shell,
skip-links, theme toggle. Deliberately excluded from this foundation
phase per spec (no Navbar yet) — folder exists so the first UI PR has
an obvious home for these pieces.
