# components/home

Composition-level sections specific to the landing page (Hero, About,
Highlights, etc.). Excluded from this foundation phase per spec.
Each section should be a small, independent Server Component by
default, promoted to a Client Component only for interaction/animation.
