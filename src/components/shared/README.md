# components/shared

Small, reusable, domain-agnostic building blocks that are more opinionated
than `ui/` primitives but still used across 2+ feature areas - e.g. a
`Container`, `SectionHeading`, or `AnimatedInView` wrapper. If something
is only used in one feature folder, it belongs there instead.
