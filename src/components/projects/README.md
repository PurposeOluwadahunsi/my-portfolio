# components/projects

Everything related to rendering project case studies: project cards,
project detail layouts, filters/tags. Pairs with `src/data` (project
content) and `src/types/project.ts` (the Project shape).
